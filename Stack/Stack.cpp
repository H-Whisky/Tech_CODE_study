#include <iostream>
#include <stack>
using namespace std;

/*

int main() {
	int tmp = 0;
	stack<int> istack;//定义为int类型的函数
	//将10，20，30依次入栈
	istack.push(10);
	istack.push(20);
	istack.push(30);
	//将"栈顶元素"赋值给tmp,并删除"栈顶元素"
	istack.pop();
	//只将"栈顶"赋值给tmp,不删除该元素
	tmp = istack.top();
	istack.push(40);
	while (!istack.empty()) {
		tmp = istack.top();
		istack.pop();
		cout << tmp << endl;
	}
	cin.get();
	return (0);

}
*/